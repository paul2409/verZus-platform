# VERZUS M0 Documentation Pack

Copy the `docs` directory into the root of the new `verzus-platform` repository.

Recommended sequence:

1. Review `vision.md`.
2. Review `v1-scope.md` and `deferred-features.md`.
3. Review `domain-model.md` and `state-machines.md`.
4. Review `feature-boundaries.md`, `routes.md`, and `permissions.md`.
5. Review architecture and operational documents.
6. Complete `M0-CHECKLIST.md`.

Do not generate full screen references or write application code until M0 is approved.
