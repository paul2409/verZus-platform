# M1 Exit Report

Complete this report before starting M2.

## Repository

- [ ] `verzus-platform` is a new Git repository
- [ ] `verzus-legacy` is a sibling directory
- [ ] M0 documents are committed
- [ ] no legacy feature directory was copied
- [ ] `.env.local` is ignored

## Local validation

- [ ] `npm ci` succeeds after deleting `node_modules`
- [ ] `npm run env:check:test` passes
- [ ] `npm run format:check` passes
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `npm run check:boundaries` passes
- [ ] `npm run build` passes
- [ ] `npm run test:e2e` passes on mobile and desktop Chromium

## CI and operations

- [ ] CI passes on a pull request
- [ ] standalone build artifact is uploaded
- [ ] `/api/health` reports environment and release
- [ ] rollback runbook was reviewed
- [ ] build-failure runbook was reviewed
- [ ] no secrets were committed

## Product discipline

- [ ] no full product screen was implemented
- [ ] M2 begins with visual foundation boards
- [ ] 390px mobile references will be approved before full screen code

## Approval

- Owner:
- Date:
- Commit SHA:
- Decision: approved / blocked
- Blocking issues:
