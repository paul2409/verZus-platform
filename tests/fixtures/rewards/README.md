# Reward fixtures

Runtime reward data must come from PostgreSQL. Deterministic legacy reward scenarios, when retained for test reconstruction, are stored as `.txt` snapshots in this directory and must never be imported by application code.
