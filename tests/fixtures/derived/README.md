# Derived-experience fixtures

Legacy deterministic Search, Play, Notification, and Activity records are retained here only as historical test/reference material.
They must never be imported by `src/app`, production API routes, or feature server services.
